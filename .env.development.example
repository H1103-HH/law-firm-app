# 开发环境配置

# 此文件用于开发环境
# 生产环境请参考 .env.local.example 文件

# 后端 API 地址（开发环境）
# 使用 Vite 代理，实际请求会被代理到 http://localhost:3000
PROJECT_DOMAIN=/api

# 说明：
# 1. 开发环境使用 `/api` 作为前缀
# 2. Vite 会将 `/api` 请求代理到 `http://localhost:3000`
# 3. 配置文件位于 config/index.ts 的 server.proxy 部分
# 4. 生产环境需要配置完整的 HTTPS 域名，参考 .env.local.example
